---
vc_hostname: vcenter.lab.local
vc_username: administrator@vsphere.local
vc_password: xxxxx
#vc_resource_pool: ocp4.example.com
vc_resource_pool: "{{ ext_resource_pool }}"
#vc_folder: /LAB/vm/redhat/openshift/ocp4-example
vc_folder: "{{ ext_vc_folder }}" 
vc_template: template_slackware15
vc_datastore: "{{ ext_vc_datastore }}"
vm_name: "{{ ext_vm_name }}"
vm_network_range: "{{ ext_vm_network_range }}"
vm_network1: "{{ ext_vm_network1 }}"
#vm_network2: "{{ ext_vm_network2 }}"
vm_ip1: "{{ ext_vm_ip1 }}"
#vm_ip2: "{{ ext_vm_ip2 }}"
vm_gw: "{{ ext_vm_gw }}"
vm_dns1: "{{ ext_vm_dns1 }}"
vm_dns_search: "{{ ext_vm_dns_search }}"
# dhcp
#dhcp_subnet: "{{ ext_dhcp_subnet }}"
#dhcp_range_start: "{{ ext_dhcp_range_start }}"
#dhcp_range_end: "{{ ext_dhcp_range_end }}"
#dhcp_broadcast_ip: "{{ ext_dhcp_broadcast_ip }}"
# dns 
#dns_reverse_arpa: "{{ ext_dns_reverse_arpa }}"
#dns_zone_serial: "{{ ext_dns_zone_serial }}"
